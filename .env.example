# Aurave ERP – Copy to .env and set values. NEVER commit .env to Git.
# Production: set APP_ENV=production, APP_DEBUG=0

APP_NAME=Aurave ERP
APP_ENV=production
APP_DEBUG=0
APP_URL=http://localhost/Aurave/public

# Database (set in environment; no defaults for production)
DB_HOST=localhost
DB_NAME=aurave_erp
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

SESSION_LIFETIME=7200
DEFAULT_TIMEZONE=UTC

# Local development: set APP_ENV=development and APP_DEBUG=1 in your .env
